<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <style>
        body { background-color: #1A1A1A; color: #fff; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; padding: 20px; text-align: center; }
        .header h1 { color: #C5A059; margin-bottom: 10px; }
        .player-container { max-width: 900px; margin: 0 auto 30px auto; }
        .player-container iframe { width: 100%; height: 500px; border: none; border-radius: 8px; box-shadow: 0 0 15px rgba(197, 160, 89, 0.3); }
        .watch-btn { display: inline-block; margin-top: 20px; padding: 15px 30px; background: #C5A059; color: #1A1A1A; text-decoration: none; font-weight: bold; border-radius: 8px; font-size: 18px; }
        .details { max-width: 800px; margin: 20px auto; background: #111; padding: 20px; border-radius: 8px; border: 1px solid #C5A059; display: flex; gap: 20px; text-align: right; }
        .poster { width: 200px; border-radius: 8px; flex-shrink: 0; }
        .back-link { display: inline-block; margin-top: 20px; color: #C5A059; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h1><%= movie.title %></h1>
    </div>

    <div class="player-container">
        <!-- محاولة تشغيل الفيديو مباشرة داخل الموقع -->
        <iframe src="https://www.2embed.cc/embed/<%= movie.id %>" allowfullscreen frameborder="0" allow="fullscreen; encrypted-media; autoplay; picture-in-picture"></iframe>
        
        <br><br>
        <!-- الزر الذكي: إذا لم يظهر الفيديو بالأعلى، اضغط هنا ليفتح في نافذة ديسكورد المنبثقة -->
        <a href="https://vidsrc.to/embed/movie/<%= movie.id %>" target="_blank" class="watch-btn">
            ▶ شاهد الفيلم كاملاً (في نافذة منبثقة)
        </a>
    </div>

    <div class="details">
        <% if (movie.poster_path) { %>
            <img src="/proxy-image?path=<%= encodeURIComponent(movie.poster_path) %>" class="poster">
        <% } %>
        <div>
            <p><strong>تاريخ الإصدار:</strong> <%= movie.release_date || 'غير معروف' %></p>
            <p><strong>التقييم:</strong> ⭐ <%= movie.vote_average ? movie.vote_average.toFixed(1) : 'N/A' %></p>
            <p style="margin-top: 15px; line-height: 1.6;"><strong>القصة:</strong> <%= movie.overview || 'لا تتوفر تفاصيل لهذا الفيلم حالياً.' %></p>
        </div>
    </div>

    <a href="/" class="back-link">العودة للرئيسية</a>
</body>
</html>